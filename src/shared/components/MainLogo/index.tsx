import { Link } from 'react-router-dom';

const MainLogo = () => {
  return (
    <Link to="/" className="font-bold text-3xl text-gray-800" title="Product Store">
      Task App
    </Link>
  );
};

export default MainLogo;
